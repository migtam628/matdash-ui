# Contributing to MatDash UI

Thanks for contributing.

## Development

```bash
npm install
npm run dev
```

## Before opening a pull request

```bash
npm run typecheck
npm run build:lib
```

Keep components written in TypeScript/TSX and plain CSS. Prefer reusable props, accessible native semantics, keyboard support, and design-token based styling.
