import { useState } from 'react'; import { Calendar } from '../Calendar/Calendar'; import { Popover } from '../Popover/Popover'; import './DatePicker.css';
export interface DatePickerProps { value?:Date|null; defaultValue?:Date|null; onChange?:(date:Date)=>void; label?:string; placeholder?:string; minDate?:Date; maxDate?:Date; disabled?:boolean; className?:string; }
const format=(d:Date)=>d.toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'});
export function DatePicker({value:controlled,defaultValue=null,onChange,label,placeholder='Select date',minDate,maxDate,disabled=false,className=''}:DatePickerProps){const [internal,setInternal]=useState<Date|null>(defaultValue);const [open,setOpen]=useState(false);const value=controlled !== undefined ? controlled : internal;const select=(d:Date)=>{if(controlled===undefined)setInternal(d);onChange?.(d);setOpen(false)};return <div className={`md-datepicker ${className}`}>{label&&<span className="md-datepicker__label">{label}</span>}<Popover open={open} onOpenChange={setOpen} trigger={<button type="button" className="md-datepicker__trigger" disabled={disabled}><span>{value?format(value):placeholder}</span><span aria-hidden>▣</span></button>}><Calendar value={value} onChange={select} minDate={minDate} maxDate={maxDate}/></Popover></div>}
