import type { ReactNode } from 'react'; import './RadialProgress.css';
export interface RadialProgressProps { value:number; max?:number; size?:number; strokeWidth?:number; tone?:'primary'|'success'|'info'|'warning'|'danger'; label?:ReactNode; showValue?:boolean; formatValue?:(value:number)=>ReactNode; className?:string; }
export function RadialProgress({value,max=100,size=128,strokeWidth=10,tone='primary',label,showValue=true,formatValue=(v)=>`${Math.round(v)}%`,className=''}:RadialProgressProps){const safe=Math.max(0,Math.min(max,value));const pct=max?safe/max*100:0;const r=(size-strokeWidth)/2;const c=2*Math.PI*r;const dash=c*(pct/100);return <div className={`md-radial md-radial--${tone} ${className}`} style={{width:size,height:size}}><svg width={size} height={size} viewBox={`0 0 ${size} ${size}`}><circle className="md-radial__track" cx={size/2} cy={size/2} r={r} fill="none" strokeWidth={strokeWidth}/><circle className="md-radial__value" cx={size/2} cy={size/2} r={r} fill="none" strokeWidth={strokeWidth} strokeLinecap="round" strokeDasharray={`${dash} ${c-dash}`} transform={`rotate(-90 ${size/2} ${size/2})`}/></svg><div className="md-radial__center">{showValue&&<b>{formatValue(pct)}</b>}{label&&<small>{label}</small>}</div></div>}
