import { useState, type ReactNode } from 'react'; import './TreeView.css';
export interface TreeNode { id:string; label:ReactNode; icon?:ReactNode; children?:TreeNode[]; disabled?:boolean; meta?:ReactNode; }
export interface TreeViewProps { nodes:TreeNode[]; selectedId?:string; defaultSelectedId?:string; onSelect?:(node:TreeNode)=>void; defaultExpanded?:string[]; expanded?:string[]; onExpandedChange?:(ids:string[])=>void; className?:string; }
export function TreeView({nodes,selectedId:controlledSelected,defaultSelectedId='',onSelect,defaultExpanded=[],expanded:controlledExpanded,onExpandedChange,className=''}:TreeViewProps){const [selected,setSelected]=useState(defaultSelectedId);const [internalExpanded,setInternalExpanded]=useState(defaultExpanded);const selectedId=controlledSelected??selected;const expanded=controlledExpanded??internalExpanded;const toggle=(id:string)=>{const next=expanded.includes(id)?expanded.filter(x=>x!==id):[...expanded,id];if(controlledExpanded===undefined)setInternalExpanded(next);onExpandedChange?.(next)};const choose=(n:TreeNode)=>{if(n.disabled)return;if(controlledSelected===undefined)setSelected(n.id);onSelect?.(n)};const render=(items:TreeNode[],level=1)=><ul role={level===1?'tree':'group'}>{items.map(n=>{const has=!!n.children?.length;const open=expanded.includes(n.id);return <li role="treeitem" aria-expanded={has?open:undefined} aria-selected={selectedId===n.id} key={n.id}><div className={`md-tree__row ${selectedId===n.id?'is-selected':''} ${n.disabled?'is-disabled':''}`} style={{paddingLeft:8+(level-1)*18}}>{has?<button type="button" className="md-tree__toggle" aria-label={open?'Collapse':'Expand'} onClick={()=>toggle(n.id)}>{open?'⌄':'›'}</button>:<span className="md-tree__spacer"/>}<button type="button" className="md-tree__label" disabled={n.disabled} onClick={()=>choose(n)}>{n.icon&&<span className="md-tree__icon">{n.icon}</span>}<span>{n.label}</span>{n.meta&&<em>{n.meta}</em>}</button></div>{has&&open&&render(n.children!,level+1)}</li>})}</ul>;return <div className={`md-tree ${className}`}>{render(nodes)}</div>}
