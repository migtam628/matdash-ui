import type { InputHTMLAttributes, ReactNode } from 'react'; import './Input.css';
export interface InputProps extends InputHTMLAttributes<HTMLInputElement> { label?: string; hint?: string; error?: string; leadingIcon?: ReactNode; trailing?: ReactNode; }
export function Input({ label,hint,error,leadingIcon,trailing,className='',id,...props }: InputProps){const inputId=id ?? `md-input-${label?.toLowerCase().replace(/[^a-z0-9]+/g,'-') ?? 'field'}`;return <label className={`md-field ${className}`} htmlFor={inputId}>{label&&<span className="md-field__label">{label}</span>}<span className={`md-input-wrap ${error?'md-input-wrap--error':''}`}>{leadingIcon&&<span className="md-input__icon">{leadingIcon}</span>}<input id={inputId} className="md-input" {...props}/>{trailing&&<span className="md-input__trailing">{trailing}</span>}</span>{(error||hint)&&<span className={`md-field__hint ${error?'is-error':''}`}>{error??hint}</span>}</label>}
