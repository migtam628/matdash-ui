import { useEffect, type ReactNode } from 'react'; import { X } from 'lucide-react'; import { IconButton } from '../IconButton/IconButton'; import './Drawer.css';
export interface DrawerProps{open:boolean;onClose:()=>void;title?:ReactNode;children:ReactNode;footer?:ReactNode;side?:'left'|'right';width?:number}
export function Drawer({open,onClose,title,children,footer,side='right',width=380}:DrawerProps){useEffect(()=>{if(!open)return;const fn=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[open,onClose]);return <div className={`md-drawer-layer ${open?'is-open':''}`} aria-hidden={!open}><div className="md-drawer-backdrop" onClick={onClose}/><aside className={`md-drawer md-drawer--${side}`} style={{width}}><header><h2>{title}</h2><IconButton icon={<X size={18}/>} label="Close" onClick={onClose}/></header><div className="md-drawer__body">{children}</div>{footer&&<footer>{footer}</footer>}</aside></div>}
