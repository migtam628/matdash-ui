import type { ReactNode } from 'react'; import './Sidebar.css';
export interface SidebarItem{key:string;label:ReactNode;icon?:ReactNode;badge?:ReactNode;disabled?:boolean}
export interface SidebarProps{brand?:ReactNode;items:SidebarItem[];activeKey?:string;onSelect?:(key:string)=>void;footer?:ReactNode;collapsed?:boolean;className?:string}
export function Sidebar({brand,items,activeKey,onSelect,footer,collapsed=false,className=''}:SidebarProps){return <aside className={`md-sidebar ${collapsed?'is-collapsed':''} ${className}`}>{brand&&<div className="md-sidebar__brand">{brand}</div>}<nav>{items.map(item=><button type="button" key={item.key} disabled={item.disabled} className={activeKey===item.key?'is-active':''} onClick={()=>onSelect?.(item.key)} title={typeof item.label==='string'?item.label:undefined}>{item.icon&&<span className="md-sidebar__icon">{item.icon}</span>}<span className="md-sidebar__label">{item.label}</span>{item.badge&&<span className="md-sidebar__badge">{item.badge}</span>}</button>)}</nav>{footer&&<div className="md-sidebar__footer">{footer}</div>}</aside>}
