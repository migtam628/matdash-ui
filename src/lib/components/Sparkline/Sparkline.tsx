import './Sparkline.css';
export interface SparklineProps{values:number[];color?:string;height?:number;strokeWidth?:number;fill?:boolean;ariaLabel?:string}
export function Sparkline({values,color='var(--md-chart-1)',height=56,strokeWidth=2.4,fill=false,ariaLabel='Trend'}:SparklineProps){if(values.length<2)return null;const w=180,pad=4,min=Math.min(...values),max=Math.max(...values),range=max-min||1;const pts=values.map((v,i)=>[(i/(values.length-1))*(w-pad*2)+pad,height-pad-((v-min)/range)*(height-pad*2)]);const path=pts.map(([x,y],i)=>`${i?'L':'M'} ${x} ${y}`).join(' ');const last=pts[pts.length-1];const area=`${path} L ${last?.[0]} ${height} L ${pts[0][0]} ${height} Z`;return <svg className="md-sparkline" viewBox={`0 0 ${w} ${height}`} role="img" aria-label={ariaLabel}>{fill&&<path d={area} fill={color} opacity=".08"/>}<path d={path} fill="none" stroke={color} strokeWidth={strokeWidth} strokeLinecap="round" strokeLinejoin="round"/></svg>}
